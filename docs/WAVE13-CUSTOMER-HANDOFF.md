# WAVE13 customer handoff

This handoff note was previously committed as an absolute-path symlink, which
breaks GitHub Pages artifact builds in CI.

The original external target is not part of this repository. Keep this file in
the repo as a regular markdown document so docs packaging remains stable.
